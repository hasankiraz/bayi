# bayi

This repository will hold the source code for a simple command-line tool.

## Usage

Run the script to display a greeting message:

```bash
python bayi.py --name YourName
```

Replace `YourName` with your own name to see a personalized greeting.
